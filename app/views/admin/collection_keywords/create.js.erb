if ($("#collection_collection_keywords").val().split(" ").indexOf("<%= @keyword.id %>") != -1) {
  alert("이미 추가한 키워드입니다");
} else {
  add_keyword("<%= @keyword.id %>");
  $("#keywords_list").append("<%= escape_javascript(render 'collection_keyword') %>");
  form_check($("#collection_form"));
  $("#related_collections").click();
};
$("#keyword").val("").focus();
$("#keyword_submit").attr("disabled", true);
$("#collection_keyword_form").attr("method", "get");
preview_keyword();
