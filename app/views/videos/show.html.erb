<script>
  $(window).ready(function() {
    go_seg(1);
  }).load(function() {
    go_url('video_asks');
    if (visitor_check()) {
      ga('send', 'event', '랜딩페이지', '랜딩페이지 진입', '비회원 비교영상 진입', 1);
    } else {
      go_url('video_ask', <%= @video.id %>);
    }
  });
</script>
